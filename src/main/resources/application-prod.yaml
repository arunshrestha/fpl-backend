spring:
  datasource:
    url: jdbc:postgresql://<PROD_HOST>:5432/<PROD_DB>?sslmode=require
    username: <PROD_USER>
    password: <PROD_PASS>
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false